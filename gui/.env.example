# Frontend Environment Variables

# URL del backend API (senza /api alla fine)
# Per sviluppo locale:
REACT_APP_API_URL=http://localhost:10000

# Per produzione (esempio):
# REACT_APP_API_URL=https://your-backend.onrender.com

# Altre configurazioni opzionali
REACT_APP_SOLANA_NETWORK=mainnet-beta
REACT_APP_DEBUG=false

# IMPORTANTE: SOLSCAN_API_KEY deve essere configurata sul backend (Render),
# NON nel frontend per motivi di sicurezza.
# Se vedi l'errore "SOLSCAN_API_KEY non configurata", configura la chiave
# nelle variabili d'ambiente del backend su Render.