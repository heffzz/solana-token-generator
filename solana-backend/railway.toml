[build]
cmd = "npm install"

[deploy]
cmd = "npm start"

[env]
PORT = "10000"
SOLANA_RPC_URL = "https://api.mainnet-beta.solana.com"

[healthcheck]
path = "/api/system/stats"
interval = "30s"
timeout = "10s"
retries = 3